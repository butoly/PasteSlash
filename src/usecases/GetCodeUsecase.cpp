#include "usecases/GetCodeUsecase.h"


int GetCodeUsecase::execute() {

    bool isFound;
    if (!isFound) {
        return -1;
    }

    bool isOk;
    if (!isOk){
        return -2;
    }
    //достаем из бд код

    return 0;
}